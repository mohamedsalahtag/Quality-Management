{
  "ConnectionStrings": {
    "_comment": "SECURITY: This file is in source control. Do NOT put real credentials here. Override the connection string in appsettings.Development.json (local) or appsettings.Production.json (server) -- both of which should be gitignored. You can also set the env var ConnectionStrings__Default to override at runtime.",
    "Default": "Server=__OVERRIDE_IN_ENV_OR_LOCAL_CONFIG__;Database=SharbatlyQMS;TrustServerCertificate=True;Persist Security Info=True;Connect Timeout=15;ConnectRetryCount=5;ConnectRetryInterval=3;Pooling=True;Min Pool Size=1;Max Pool Size=200"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "QMS": {
    "ImageUploadRoot": "uploads/images",
    "ThumbnailRoot": "uploads/thumbnails",
    "MaxImageSizeMB": 10,
    "_documentComment": "V39: DocumentRoot is relative to the CONTENT root (not wwwroot) so documents are never served by static-file middleware; an absolute path is also accepted. Both keys below are read by DocumentService.",
    "DocumentRoot": "App_Data/documents",
    "MaxDocumentSizeMB": 25
  }
}
